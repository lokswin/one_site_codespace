{
  "name": "OnePage Docker Compose Environment",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "web",
  "workspaceFolder": "/workspace",
  "forwardPorts": [8080],
  "postStartCommand": "chmod +x /usr/local/bin/one_site_start.sh && /usr/local/bin/one_site_start.sh"
}
